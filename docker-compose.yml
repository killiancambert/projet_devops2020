version: '3'

services:
  automate:
    build: ./automate
    volumes:
      - ./automate:/usr/src/app
    ports:
      - 5001:80

  recup_donnees:
    build: ./recup_donnees
    volumes:
      - ./recup_donnees:/usr/src/app
    ports:
      - 5000:80
    depends_on:
      - automate